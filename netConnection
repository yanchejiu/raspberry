//use 'sudo ifdown wlan0' turn off network,and use 'sudo ifup wlan0' to turn on it
//the network config is on /etc/wpa_supplicant/wpa_supplicant.conf
//can use that address add or modify wifi parament at the bottom,by following
network={
	ssid=""
	psk=""
}
